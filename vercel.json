{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build"
    }
  ],
  "scripts": {
    "build": "npm install --legacy-peer-deps && npm run build:vite"
  },
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ]
}
